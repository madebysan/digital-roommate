import Cocoa

// Shared visual constants used by remaining AppKit code (StatusBarController).
enum Styles {
    static let sidebarIconSize: CGFloat = 13
    static let statusBarIconSize: CGFloat = 16
}
